import React from 'react';
import { Text, View } from 'react-native';

const Saved = () => {
    return (
        <View className='flex-1 items-center justify-center bg-primary'>
            <Text className='text-white text-lg font-bold'>Saved Movies</Text>
        
        </View>
    );
};

export default Saved;
